name: Deploy to Oracle VM (prod)

on:
  push:
    tags:
      - 'v*'

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Build JAR
        run: mvn package -DskipTests -q

      - name: Run database migrations
        env:
          FLYWAY_URL: ${{ secrets.PROD_DB_URL }}
          FLYWAY_USER: ${{ secrets.PROD_DB_USERNAME }}
          FLYWAY_PASSWORD: ${{ secrets.PROD_DB_PASSWORD }}
        run: mvn flyway:migrate

      - name: Set up SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.ORACLE_PROD_SSH_KEY }}" > ~/.ssh/oracle-prod.key
          chmod 600 ~/.ssh/oracle-prod.key
          ssh-keyscan -H ${{ secrets.ORACLE_PROD_HOST }} >> ~/.ssh/known_hosts

      - name: Copy JAR to VM
        run: |
          scp -i ~/.ssh/oracle-prod.key \
            target/skipq-core-0.0.1-SNAPSHOT.jar \
            ubuntu@${{ secrets.ORACLE_PROD_HOST }}:/home/ubuntu/skipq/skipq-core.jar

      - name: Write env file and restart service
        run: |
          ssh -i ~/.ssh/oracle-prod.key ubuntu@${{ secrets.ORACLE_PROD_HOST }} << 'EOF'
          cat > /home/ubuntu/skipq/.env << ENVEOF
          SPRING_PROFILES_ACTIVE=prod
          PROD_DB_URL=${{ secrets.PROD_DB_URL }}
          PROD_DB_USERNAME=${{ secrets.PROD_DB_USERNAME }}
          PROD_DB_PASSWORD=${{ secrets.PROD_DB_PASSWORD }}
          JWT_SECRET=${{ secrets.JWT_SECRET }}
          RESEND_API_KEY=${{ secrets.RESEND_API_KEY }}
          RESEND_FROM_EMAIL=${{ secrets.RESEND_FROM_EMAIL }}
          DEEP_LINK_BASE_URL=${{ secrets.DEEP_LINK_BASE_URL }}
          ABLY_API_KEY=${{ secrets.ABLY_API_KEY }}
          RAZOR_TEST_ID=${{ secrets.RAZOR_TEST_ID }}
          RAZOR_TEST_SECRET=${{ secrets.RAZOR_TEST_SECRET }}
          RAZOR_PROD_ID=${{ secrets.RAZOR_PROD_ID }}
          RAZOR_PROD_SECRET=${{ secrets.RAZOR_PROD_SECRET }}
          RAZOR_WEBHOOK_SECRET_PROD=${{ secrets.RAZOR_WEBHOOK_SECRET_PROD }}
          R2_ACCOUNT_ID=${{ secrets.R2_ACCOUNT_ID }}
          R2_ACCESS_KEY_ID=${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY=${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET_NAME=skipq-images
          R2_PUBLIC_URL=https://pub-1a1d55258a2b4bd5affe723c3fed4dcd.r2.dev
          FIREBASE_PROJECT_ID=skipq-0521
          GOOGLE_APPLICATION_CREDENTIALS=/home/ubuntu/skipq/firebase-service-account.json
          OTEL_SDK_DISABLED=true
          NR_INGEST_LICENSE_KEY=${{ secrets.NR_INGEST_LICENSE_KEY }}
          NEW_RELIC_LICENSE_KEY=${{ secrets.NR_INGEST_LICENSE_KEY }}
          ENVEOF
          sudo systemctl restart skipq-core
          EOF

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
